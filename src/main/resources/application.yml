spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/simple-postgres
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate.dialect: com.example.oidtypeconversion.ByteaSQLDialect
axon:
  axon server:
    enabled: false
